/* eslint-disable @typescript-eslint/no-duplicate-enum-values */
export enum ErrorCodes {
  NOT_FOUND = 404,
  INVALID_ARGUMENT = 400,
  INTERNAL = 500,
}

export enum GrpcCodesToHttp {
  UNKNOWN = 500,
  INVALID_ARGUMENT = 400,
  DEADLINE_EXCEEDED = 504,
  NOT_FOUND = 404,
  ALREADY_EXISTS = 409,
  PERMISSION_DENIED = 403,
  UNAUTHENTICATED = 401,
  RESOURCE_EXHAUSTED = 429,
  FAILED_PRECONDITION = 412,
  ABORTED = 409,
  OUT_OF_RANGE = 400,
  UNIMPLEMENTED = 501,
  INTERNAL = 500,
  UNAVAILABLE = 503,
  DATA_LOSS = 500,
}
